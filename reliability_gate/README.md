# Reliability Gate

Reliability-gate validation and answer selection utilities.

Use the public entrypoints in `../scripts/` or follow the top-level `README.md` for the standard reproduction workflow.
